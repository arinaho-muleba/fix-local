{
  "name": "fix-local",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"eleventy --serve\" \"npx tailwindcss -i ./tailwind.css -o ./_site/css/styles.css --watch\" \"npx pagefind --site _site --serve\"",
    "build": "eleventy  && npx pagefind --site _site"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "concurrently": "^9.1.0",
    "tailwindcss": "^3.4.14"
  },
  "dependencies": {
    "fix-local": "file:",
    "pagefind": "^1.2.0"
  }
}
